#imports são links de bibliotecas que utilizamos no python, para importar dado
import random
lista = [1, 2, 3, 4]
sorteio = random.choice(lista)
print('o numero sorteado foi:', sorteio)

import math
